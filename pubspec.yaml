name: flutter_multi_dropdown
description: 'Flutter multi-select dropdown with checkboxes and built-in "Select All" option with search functionality.'
version: 0.0.5
homepage: https://github.com/NamanGajera/flutter_multi_dropdown
topics:
  - dropdown
  - multi-select
  - flutter-widget
  - selection
  - search

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

screenshots:
  - description: "Example screenshot 1"
    path: images/demo_video.gif
